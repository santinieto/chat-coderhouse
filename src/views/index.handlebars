<div class="container mt-5">
    <div class="card shadow p-4">
        <h1 class="text-center mb-4">Página de inicio</h1>

        <form id="chat-form" class="d-flex">
            <input type="text" id="chat-input" class="form-control me-2" placeholder="Escribe tu mensaje aquí">
            <button type="submit" class="btn btn-primary">Enviar</button>
        </form>

        <div id="chat-box" class="mt-4 border p-3 rounded bg-white" style="height: 300px; overflow-y: auto;">
            <!-- Mensajes del chat aparecerán aquí -->
        </div>
    </div>
</div>